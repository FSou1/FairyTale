@model ISet<Tag>

@{ var tags = Model; }

@if (tags.Count > 0) {
    <p class="tags">
        <i class="fa fa-tags"></i>
        @foreach (var tag in tags) {
            @Html.ActionLink(tag)
        }
    </p>
}